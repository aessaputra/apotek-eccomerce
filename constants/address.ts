/** Default address left accent border width in pixels. */
export const DEFAULT_ACCENT_BORDER_WIDTH = 4;

/** Background tint opacity for the default address card (4% of primary). */
export const DEFAULT_BG_TINT_OPACITY = 0.04;

/** Minimum swipe distance (px) to reveal action buttons. */
export const SWIPE_THRESHOLD = 80;

/** Total width of the revealed right action panel. */
export const SWIPE_ACTION_WIDTH = 192;

/** Individual action button width inside the swipe panel. */
export const SWIPE_BUTTON_WIDTH = 64;

/** Duration (ms) for card entrance animation (FadeInDown). */
export const ENTRANCE_DURATION_MS = 300;

/** Stagger delay (ms) between consecutive card entrance animations. */
export const ENTRANCE_STAGGER_DELAY_MS = 60;

/** Duration (ms) for card deletion animation (FadeOutLeft + height collapse). */
export const EXIT_DURATION_MS = 300;

/** Button press scale (0.97 = shrinks 3%). */
export const PRESS_SCALE = 0.97;

/** Button press opacity on active state. */
export const PRESS_OPACITY = 0.9;

/** Button press animation duration (ms). */
export const PRESS_DURATION_MS = 100;

/** Icon size (px) for the address list empty state. */
export const EMPTY_STATE_ICON_SIZE = 80;

/** Estimated height (px) of a single address card row for FlatList optimization. */
export const ADDRESS_ITEM_HEIGHT = 140;

/** Horizontal padding for the address list container. */
export const LIST_HORIZONTAL_PADDING = 20;
