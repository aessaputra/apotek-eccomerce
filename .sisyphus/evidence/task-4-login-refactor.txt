Task 4 - Login refactor evidence
Date: 2026-04-26

Files changed:
- scenes/auth/Login.tsx
- scenes/auth/useLoginForm.ts
- scenes/auth/authForm.helpers.ts
- __tests__/scenes/Login.test.tsx

Implementation notes:
- Extracted Login auth orchestration into auth-local `useLoginForm()` so the scene no longer owns low-level password sign-in branching, email-not-verified routing, OAuth cancellation handling, forgot-password navigation, or reset-success route-param consumption.
- Preserved Login visual language: existing auth card, Tamagui tokens, inputs, primary button, signup link, divider, and Google OAuth button remain in place.
- Added `Lupa Password?` inside the existing form card; it routes to `/(auth)/forgot-password` through the hook.
- Added token-based success banner for `resetSuccess`, and the hook clears the route param by replacing `/(auth)/login` after consumption.
- Hardened reset-success route-param handling so only the approved `LOGIN_RESET_SUCCESS_MESSAGE` is displayed; unapproved route-param text is ignored to avoid trusted-message injection on the Login screen.

Before/after complexity note:
- Before: `Login.tsx` owned form state, validation, password service branching, verify-email redirect, OAuth service branching, and UI rendering in one component.
- After: `Login.tsx` renders UI and delegates auth-specific state/events to `scenes/auth/useLoginForm.ts`; service calls still go through `signInWithPassword` and `signInWithGoogle` only.

Command: npm run test -- --runTestsByPath __tests__/scenes/Login.test.tsx __tests__/scenes/authForm.helpers.test.ts
Result: PASS
Output excerpt:
PASS __tests__/scenes/authForm.helpers.test.ts
PASS __tests__/scenes/Login.test.tsx (8.078 s)
  <Login />
    ✓ shows the required-field message for an empty form
    ✓ shows the invalid-email message before calling the service
    ✓ redirects unverified email logins with the trimmed email param
    ✓ ignores Google OAuth cancellation without showing an error
    ✓ shows Google OAuth service errors returned by the auth service
    ✓ shows the generic login error when password sign-in throws
    ✓ navigates to forgot password from the Login form card
    ✓ shows and clears the reset success route-param message once
    ✓ ignores unapproved reset success route-param text
Test Suites: 2 passed, 2 total
Tests: 15 passed, 15 total

Command: npm run format:check
Result: PASS
Output excerpt:
Checking formatting...
All matched files use Prettier code style!

LSP diagnostics:
- scenes/auth/useLoginForm.ts: No diagnostics found
- scenes/auth/authForm.helpers.ts: No diagnostics found
- __tests__/scenes/Login.test.tsx: No diagnostics found
- scenes/auth/Login.tsx: one pre-existing TypeScript hint for deprecated Image `resizeMode`; no errors/warnings introduced.

Code pattern scans:
- No direct Supabase references in changed Login source/test files.
- No `as any`, `@ts-ignore`, or `@ts-expect-error` in changed Login source/test files.
- `console.log` appears only in the preserved Google OAuth exception branch and remains guarded by `if (__DEV__)` in `useLoginForm.ts`.
- Unapproved `resetSuccess` route-param text is covered by test and not rendered.

Additional command: npx tsc --noEmit
Result: FAIL due to known unrelated pre-existing failures.
Observed unrelated files: __tests__/components/ProductCard.test.tsx, order hook/service tests, app/(tabs)/_layout.tsx, scenes/profile/addressFormFlow.ts.
Modified Login files did not appear in the TypeScript failure output.
