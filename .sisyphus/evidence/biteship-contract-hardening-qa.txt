
# biteship-contract-hardening-qa.txt

## Task 6 Deployment and QA Status (2026-04-25T16:02:22Z)

### Deployment Status

Deployment status: BLOCKED by Supabase CLI authentication.

Attempted deploy command from `/home/coder/dev/pharma/admin-panel`:

```bash
npx supabase --workdir "/home/coder/dev/pharma/admin-panel" functions deploy biteship --project-ref ibmpikevzfuqtfpdpkyy --use-api --no-verify-jwt && npx supabase --workdir "/home/coder/dev/pharma/admin-panel" functions deploy process-webhook-side-effects --project-ref ibmpikevzfuqtfpdpkyy --use-api --no-verify-jwt
```

Result: exit `1`, exact blocker:

```text
Access token not provided. Supply an access token by running supabase login or setting the SUPABASE_ACCESS_TOKEN environment variable.
Try rerunning the command with --debug to troubleshoot the error.
```

### JWT Preservation

Current Supabase MCP function settings before and after the blocked attempt:
- `biteship`: `verify_jwt=false`, version `61`.
- `process-webhook-side-effects`: `verify_jwt=false`, version `11`.

The intended deploy flags included `--no-verify-jwt` for both functions to preserve those settings. Since the deploy was blocked before rollout, existing remote settings remain unchanged.

### SQL / Configuration QA

Boolean-only SQL verification returned all required booleans `TRUE`:
- origin area present
- origin postal present
- origin latitude present
- origin longitude present
- enabled couriers include `gojek`, `grab`, and `lalamove`

### Runtime Logs

Supabase edge-function logs were inspected. Recent entries show:
- existing `biteship` version `61` responding with `OPTIONS | 200` and `POST | 200`.
- recurring `reconcile-pending-midtrans-payments` version `14` `POST | 200` activity.

No new deployment logs exist for `biteship` or `process-webhook-side-effects` because deployment was blocked by missing CLI auth.

### Live Invocation Decision

No live mutation or provider-backed validation invocation was run.

Rationale:
- `process-webhook-side-effects` requires service-role bearer authorization and processes webhook/order side effects; invoking it could mutate fulfillment state.
- `biteship` validation paths can call live Biteship provider endpoints and require configured secrets; safe contract behavior is already covered by focused helper tests.
- Deployment did not complete, so invoking the old deployed function would not validate the new local hardening.

### Local QA Result

Local QA passed:
- admin-panel focused Biteship helper tests: 50 tests passed.
- admin-panel `pnpm build`: passed.
- frontend focused shipping/session tests: 17 tests passed.
- frontend `npm run lint`: passed.
- frontend `npm run format:check`: passed.
- frontend generated `/supabase` tree remains absent.
