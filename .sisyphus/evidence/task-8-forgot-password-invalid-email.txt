# Task 8 Forgot Password invalid-email evidence

## Client-side validation assertions
- Empty email shows `Email wajib diisi.` and does not call `requestPasswordReset`.
- Invalid email shows `Format email tidak valid.` and does not call `requestPasswordReset`.
- The email is trimmed through `normalizeAuthEmail(...)` before successful service calls.
- The submit button enters a loading/disabled state while the async reset request is pending to prevent duplicate submissions.

## Targeted validation test output

Command:

```bash
npm run test -- --runTestsByPath __tests__/scenes/ForgotPassword.test.tsx
```

Output excerpt:

```text
PASS __tests__/scenes/ForgotPassword.test.tsx (6.304 s)
  <ForgotPassword />
    ✓ shows the required-email message for an empty form without calling the service (111 ms)
    ✓ shows the invalid-email message before calling the service (116 ms)
    ✓ sets the submit button loading state and disables duplicate submissions while pending (172 ms)
    ✓ shows generic success copy for successful reset requests using the trimmed email (143 ms)

Test Suites: 1 passed, 1 total
Tests:       8 passed, 8 total
```

## Changed-file diagnostics

`scenes/auth/ForgotPassword.tsx`:

```text
hint[typescript] (6385) at 146:18: 'resizeMode' is deprecated.
```

`__tests__/scenes/ForgotPassword.test.tsx`:

```text
No diagnostics found
```

## Format/lint outputs

`npm run format:check`:

```text
> apotek-ecommerce@3.0.0 format:check
> prettier --check "**/*.{ts,tsx}"

Checking formatting...
All matched files use Prettier code style!
```

`npm run lint`:

```text
> apotek-ecommerce@3.0.0 lint
> expo lint
```
