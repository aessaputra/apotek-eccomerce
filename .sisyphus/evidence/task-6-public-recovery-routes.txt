Task 6 evidence — public recovery routes

Verification:
- `grep` confirmed `PROTECTED_ROUTE_GROUPS` remains `['(tabs)', 'cart', 'product-details']` in `app/_layout.tsx`.
- `app/(auth)/forgot-password.tsx` contents: `export { default } from '@/scenes/auth/ForgotPassword';`
- `app/(auth)/reset-password.tsx` contents: `export { default } from '@/scenes/auth/ResetPassword';`
- `app/(auth)/_layout.tsx` now registers `login`, `forgot-password`, `reset-password`, `signup`, and `verify-email`.

Implementation note:
- Temporary null-returning scene placeholders were added only to keep the new routes compile-safe until the dedicated UX tasks land.
