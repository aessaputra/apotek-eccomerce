# Task 5 Register Verify Email Evidence

Date: 2026-04-26

## Behavior preserved
- SignUp still trims email before calling `signUp`.
- SignUp still routes to `/(auth)/verify-email` with `buildVerifyEmailRouteParams(trimmedEmail)` when Supabase returns a user without a session.
- SignUp still does not route to verify-email when Supabase returns a session.

## Covered by targeted tests
Command:

```text
npm run test -- --runTestsByPath __tests__/scenes/SignUp.test.tsx
```

Relevant passing assertions:

```text
✓ redirects users without a session to verify email using the trimmed email param
✓ does not redirect to verify email when sign-up returns a session
```

Verify-email redirect assertion:

```text
expect(mockPush).toHaveBeenCalledWith({
  pathname: '/(auth)/verify-email',
  params: { email: 'new@example.com' },
});
```

Session-success assertion:

```text
expect(mockPush).not.toHaveBeenCalledWith({
  pathname: '/(auth)/verify-email',
  params: { email: 'session@example.com' },
});
```

## Refactor-specific note
The previous empty `if (data?.session) {}` branch was removed after the session-success characterization test continued passing. The replacement condition is explicit: verify-email routing only runs when `data?.user && !data.session`.
