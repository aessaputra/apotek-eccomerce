import { Card, styled } from 'tamagui';

export const OrderSectionCard = styled(Card, {
  bordered: true,
  size: '$4',
  backgroundColor: '$surface',
  borderColor: '$surfaceBorder',
  marginHorizontal: '$4',
  marginBottom: '$3',
});

export default OrderSectionCard;
